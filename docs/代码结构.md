# 代码结构

> 一页导览：`src/` 目录职责、依赖方向、常用入口，以及两类**工程约定**（内容与数据、测试与质量闸）。
> 目录约定变更时同步更新本文件与 `AGENTS.md`；系统级决策见 `adr/`（索引 `adr/README.md`）。

## 目录一览

```
src/
  index.ts          入口（组装一局并跑主循环）
  game.ts           Game 容器 + createGame（初始化显式步骤）
  types.ts          共享类型（Card/Player/GameState/CardLocation/CardType/CardTag…）
  test-utils.ts     测试辅助（freshGame / giveHand / makeUniqueCard / equipAt / placeJudgment / placeIn）

  events/           事件系统（ADR-0001）
    GameEvent.ts      事件对象与生命周期（id/endId/clear 钩子/事件栈/onClear）
    TriggerSystem.ts  发布订阅注册表（on/off/trigger）
    history.ts        历史范围查询（findEventSince）
    types.ts          事件名常量 + 各事件数据类型
    index.ts          统一导出

  position/         位置模型（牌在哪、怎么移动；ADR-0002 / ADR-0003）
    cardArea.ts       受控容器 CardArea + 集中索引（一牌一位置）
    move.ts           实体牌层低层物理移动（movePhysical；跨容器唯一物理写点 + 离开驻留区钩子）
    cardActions.ts    实体牌层公开操作（moveCards 带驻留区终点守卫 / 牌堆原语 / 判定 / 语义封装）
    cardAreaCheck.ts  对账不变量（verifyCardState / assertCardState，含 UC ↔ 实体牌双向/单向对账）
    usedCards.ts      UsedCard 层：UC 实体（id + 规则身份 + physicalCards + loc/seq）+ 存储 + 纯谓词
    usedCardActions.ts UsedCard 层业务：钩子装载 / create·enter·move·exit·settle / equipCard / playUsedCard
    areas.ts          规则语义"区域"（手牌/装备/判定）查询辅助

  effects/          效果与技能（规则行为的统一模型；ADR-0004）
    effects.ts        Effect 五形态 + defineSkill/registerBareEffect + 技能实例 + 归属判定
    skills.ts         装载与分发（installEffects / dispatchTriggered；ADR-0005）
    persistentEffects.ts  常驻效果查询面（effectRegistry.sum/has）
    conversions.ts    转化效果查询面

  decision/         决策与窗口（玩家/AI 输入层；ADR-0007）
    selection.ts      选择会话原语（SelectionPlan/Step/Session）
    choose.ts         选择步骤工厂 + ask 家族 + 规则可选集计算
    useWindow.ts      通用用牌窗口（动作选择 + continuation）
    playChoices.ts    出牌阶段动作编排（牌/主动效果/转化）
    responses.ts      响应窗口（候选构建 + 执行）

  flow/             流程与结算
    gameFlow.ts       game/round/turn/phase 边界 + 出牌/弃牌流程 + 判定阶段（读 UC；ADR-0008）
    useCard.ts        使用牌流程（目标阶段 + 单目标生效事件；ADR-0006）
    life.ts           伤害/回复/失去体力/濒死/死亡（GameOverError）
    respond.ts        响应流程（杀→闪、决斗、南蛮、万箭、桃、无懈入口）
    distance.ts       座位距离与攻击范围
    display.ts        控制台展示

  content/          内容定义与注册（引擎的"数据/内容"面）
    cardRegistry.ts   卡牌定义注册表（CardDef）
    heroRegistry.ts   武将定义注册表（HeroDef）
    info.ts           规则文本（info）数据源：按名字查 docs 标包数据
    deck.ts + standardDeck.json  标准牌堆数据映射（108 张）
    cards/            基本牌 / 锦囊 / 装备 / 延时锦囊定义与效果注册
    heroes/           25 位标包武将（技能效果注册 + 各武将测试）
```

## 依赖方向

- **两层边界**（`adr/0003`）：实体牌层（`Card`/`CardArea`/选择/`moveCards`）**不认识 UsedCard**；
  UsedCard 层（`usedCards.ts` + `usedCardActions.ts`）是装备槽/判定区的唯一写入者。
  两层唯一耦合 = 物理层在实体牌离开驻留区时调用 `game.usedCardHooks`。
- 依赖方向：`move.ts` ← `cardActions.ts` / `usedCardActions.ts` ← `flow/useCard.ts`（无环）。
- `content/` → 引擎各簇（内容只注册，不反向被引擎依赖；唯一例外是 `game.ts` 以副作用 import 触发注册与初始装载）。
- `events/` 是最底层，不依赖其它簇；`position/` 依赖 `events/` 与 `types`。
- `effects/` 依赖 `events/`、`position/`（效果行为里会移动牌）；`decision/`、`flow/` 在两者之上。
- 入口 `index.ts` 只做组装；`game.ts` 负责一局的生命周期起点。

## 常用入口

| 想做什么 | 看哪里 |
|---|---|
| 建一局 / 初始化步骤 | `game.ts` → `createGame`（步骤见注释；阶段 4 身份场、阶段 5 rng 接入点已标） |
| 注册技能/效果 | `effects/effects.ts` → `defineSkill` / `registerBareEffect`（技能 = 元数据 + 效果集合） |
| 本局装载效果 | 已内置在 `createGame`（`effects/skills.ts` 的 `installEffects`、`installUsedCardHooks`，均幂等） |
| 移动实体牌 | `position/cardActions.ts` → `moveCards`（唯一跨容器通道；**装备槽/判定区终点硬报错**；顶/底用 putTop/putBottom） |
| 进入/迁移/退出 UC | `position/usedCardActions.ts` → `enterUsedCard` / `moveUsedCard` / `exitUsedCard` / `settleUsedCard`（实体牌跟随） |
| 使用一张牌 | `flow/useCard.ts` → `useCard`（目标阶段 → 逐目标 cardEffect → 清理处理区；`useVirtualCard` 是技能构造"视为使用"的校验入口） |
| 打出一张牌 | `position/usedCardActions.ts` → `playUsedCard`（响应窗口；同样走处理区） |
| 查牌的位置 | `position/cardActions.ts` → `getCardArea`（集中索引） |
| 查 UC | `game.usedCards`（`ofCard` 倒查 / `at(loc)` 容器视图 / `all`） |
| 查规则文本 | `content/info.ts` → `skillInfo` / `cardInfo`（或 `Skill.info` / `CardDef.info`） |
| 玩家决策 | `decision/` 各模块（ask 家族/选择计划/用牌窗口/响应窗口） |
| 事件历史查询 | `events/history.ts` → `findEventSince` |
| 对账/调试不变量 | `position/cardAreaCheck.ts` → `verifyCardState` / `assertCardState` |

## 约定一：内容与数据

（结论来源见 `经验与红线.md` 2.8 的取舍规律：配置一旦需要引用代码或包含逻辑，代码配置全面胜出。）

- **内容定义 = TS 代码**：武将/卡牌/技能都引用效果函数，注册表模式本身就是代码配置；
  军争/神话再临接入 = 新内容包 TS 代码，不是 JSON 装载。
- **牌堆 = 纯数据**：`src/content/standardDeck.json`（108 张、32 种），与卡牌定义分离；
  军争牌堆接入时统一格式。理由是它无引用、规格规整、可由工具生成。
- **`docs/*.json` 的角色**：`标包卡牌/标包武将` 是**规则文本（info）的唯一事实来源**（运行时按名字查，
  见 `content/info.ts`）；`标包牌堆/军争篇*/神话再临*` 是规划与迁移参考数据，除 info 外不进运行时。
- **`info` 只有技能与卡牌有**（effect 不需要），字段与 `name` 同级（`Skill.info` / `CardDef.info`）；
  省略时由 `defineSkill` / `cardRegistry.register` 自动填充。
- **i18n**：单语先行，不预付多语言成本（真要时参考 FreeKill 的按包翻译表）。
- **插件内容不碰渲染层**：内容的表达面只有 label/group/prompt 这类声明式字段（见 `adr/0007`）。

## 约定二：测试与质量闸

- **测试与被测模块同目录**（`src/<cluster>/xxx.test.ts`；`content/heroes/*.test.ts` 随武将），
  **测试随内容走**：新增武将/机制必须带测试（FreeKill"技能自带测试"的精神 + typed 的形式）。
- **两道闸门**：`pnpm exec tsc --noEmit` 干净 + `pnpm exec vitest run` 全绿；结构重构后再加一次
  `pnpm exec tsx src/index.ts` 整局冒烟（会跑完整局，覆盖交互路径）。
- **行为保持（有边界）**：重构默认保持规则语义与外部可观测行为，测试尽量机械更新；
  结构/API 级大重构可按新设计彻底改造并重写测试，但须**显式声明不兼容范围与行为变化**，
  禁止为迁就旧行为留兼容壳（见 `AGENTS.md`）。
- **对账不变量进测试**：`verifyCardState`/`assertCardState`（一牌一位置 + id 唯一 + UC 绑定）
  在涉及位置/UC 的测试里作为 sanity net 使用。
- **测试边界**：核心引擎测试不依赖任何内容包；内容包自带测试随包分发（插件生态）。
- **重演一致性测试**（阶段 6 落地回放时）：同 seed + 同选择日志双跑、比对状态一致——
  行为保持的终极自动化形态。
- **平台无关核心**：引擎不依赖 Node/DOM 专有 API（headless + "规则代码不读 DOM"红线）；
  平台差异（RNG/存储/网络/时钟）走 DI service 注入（阶段 5）。

